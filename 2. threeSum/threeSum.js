// Given an array of integers, return indices of the three numbers such that they add up to a specific target.
// You may assume that each input would have exactly one solution, and you many not use the same element twice.

/*
 * @param {number[]} numsArray
 * @param {number} target
 * @return {number[]}
 */

var threeSum = function(numsArray, target) {
	return;
};